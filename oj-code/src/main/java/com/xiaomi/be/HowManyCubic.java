package com.xiaomi.be;

public class HowManyCubic {
    private static String solution(String line) {
        Integer target = Integer.parseInt(line);
        while (target > 0) {

        }
        int lowerCubic = (int) Math.pow(target, 1 / 3);

        // 返回处理后的结果
        return "";
    }

    public static void main(String[] args) {
        System.out.println((int) Math.pow(17, 1 / 3));
    }
}
